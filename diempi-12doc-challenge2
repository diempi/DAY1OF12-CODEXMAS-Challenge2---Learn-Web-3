// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract ChristmasGreeting {

    // Function to add "Merry Christmas" to a given string
    function addChristmasGreeting(string memory name) public pure returns (string memory) {
        return string(abi.encodePacked("Merry Christmas, ", name, "!"));
    }
}
